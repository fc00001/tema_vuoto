{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "id": "primary_link_list",
      "type": "link_list",
      "label": "Menu principale",
      "default": "main-menu"
    }
  ]
}
{% endschema %}

<nav cl<nav>
<a href=“/”>{{ shop.name | escape }}</a>
<ul> {%- for link in linklists.main-menu.links -%} 
  <li>
  <a href=“{{ link.url }}”>{{ link.title | escape }}</a>
  </li>
  {%- endfor -%}  </ul>
<ul> <li class=“nav-item”> <a class=“nav-link” href=“/search”>Cerca</a>
  </li> {%- if shop.customer_accounts_enabled -%} {%- if customer -%} <li>
  <a href=“/account”>{{ customer.first_name | escape }}</a> </li> {%- else -%} <li>
  <a href=“/account/login”>Log in</a> </li> {%- endif -%} {%- endif -%} <li>
  <a href=“/cart”>Carrello ({{ cart.item_count }})</a>
  </li>
</ul>
</nav>


{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
